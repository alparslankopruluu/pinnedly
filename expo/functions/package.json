{
  "name": "draft-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^5.0.5",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}